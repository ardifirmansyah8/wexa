# Frontend build-time config.
#
# Leave VITE_API_BASE empty for local dev (Vite proxies /api to localhost:8000).
# For a hosted deploy, set it to your deployed backend origin, e.g.
#   VITE_API_BASE=https://moviegraph-api.onrender.com
VITE_API_BASE=
