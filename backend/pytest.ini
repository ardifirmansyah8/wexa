[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
# Silence third-party (starlette/fastapi) deprecation noise on Python 3.14;
# our own code is warning-clean.
filterwarnings =
    ignore::DeprecationWarning
